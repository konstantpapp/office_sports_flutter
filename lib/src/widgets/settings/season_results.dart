import 'package:flutter/material.dart';

class SeasonResults extends StatelessWidget {
  @override
  Widget build(context) {
    return Container(
      child: const Text('Season Results'),
    );
  }
}
